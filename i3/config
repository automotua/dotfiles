# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# use win / CMD key as mod
set $mod Mod4

# use alt key as mod
# TODO use only left alt key as mod
#set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:Monaco 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# open new terminal window
#bindsym $mod+Return exec terminator
bindsym $mod+Return exec --no-startup-id terminator

# kill focused window
bindsym $mod+q kill

# start rofi (a program launcher)
bindsym $mod+g exec --no-startup-id "rofi -show window"
bindsym $mod+d exec --no-startup-id "rofi -show run"
bindsym $mod+w exec --no-startup-id "rofi -show Workspace"

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# left & right arrow keys are used for workspace switching
bindsym $mod+a workspace prev
bindsym $mod+f workspace next
bindsym $mod+Down focus down
bindsym $mod+Up focus up

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+Shift+semicolon split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
#bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+c focus child

# rename workspaces
exec --no-startup-id i3-msg 'rename workspace 1 to "1: Todo"'
exec --no-startup-id i3-msg 'rename workspace 2 to "2: Browser"'
exec --no-startup-id i3-msg 'rename workspace 3 to "3: Code"'
exec --no-startup-id i3-msg 'rename workspace 4 to "4: Preview"'
exec --no-startup-id i3-msg 'rename workspace 5 to "5: Terminal"'

# switch to workspace
bindsym $mod+1 workspace number "1: Todo"

bindsym $mod+2 workspace number "2: Browser"
bindsym $mod+b workspace number "2: Browser"

bindsym $mod+3 workspace number "3: Code"
bindsym $mod+o workspace number "3: Code"

bindsym $mod+4 workspace number "4: Preview"

bindsym $mod+5 workspace number "5: Terminal"
bindsym $mod+semicolon workspace number "5: Terminal"

bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# workspace back and forth
bindsym $mod+Tab workspace back_and_forth

# rename workspaces on the fly by pressing mod+n
bindsym $mod+n exec i3-input -F 'rename workspace to "%s"' -P 'Rename workspace (remember to type number): '

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"

mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  status_command i3status --config ~/.i3status.conf
  position top
  colors {
    background #002b36
    statusline #ffffff
    separator  #ffffff
    focused_workspace  #b58900 #b58900 #ffffff
    active_workspace   #586e75 #586e75 #ffffff
    inactive_workspace #073642 #002b36 #ffffff
    urgent_workspace   #dc322f #dc322f #fdf6e3
  }
}

# colors                border      background   text         indicator
client.focused          #002b36     #00a9ff      #fdf6e3      #f25004
client.focused_inactive #002b36     #4a2f50      #839496      #efb092
client.unfocused        #002b36     #4a2f50      #839496      #efb092
client.urgent           #002b36     #dc322f      #fdf6e3      #002b36

# auto-lock screen after 5 minutes
#exec --no-startup-id /usr/local/bin/locker -time 5 &

# lock screen with i3lock (xset command will really put the screen to standby instead of just turn screen black)
bindsym $mod+Ctrl+l exec --no-startup-id "i3lock -c 000000 && xset dpms force off"

exec --no-startup-id "feh --bg-scale /usr/local/google/home/junqiangl/wallpapers/house.jpg"

new_window pixel 2

# Swap current workspaces of the 2 monitors
#bindsym $mod+Shift+s exec /usr/local/google/home/junqiangl/.i3/workspace-swap.py

# Sreen brightness controls
#bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
#bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Volume controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && killall -SIGUSR1 i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && killall -SIGUSR1 i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && killall -SIGUSR1 i3status

# Add gLinux Status Indicator to the i3 status bar
exec --no-startup-id /usr/share/goobuntu-indicator/goobuntu_indicator.py

# Quasi Fibonacci dynamic tiling, i.e. automatic layout like this:
# ------------
# |      | B |
# |  A   |---|
# |      | C |
# ------------
default_orientation vertical # start in opposite orientation from your monitor
for_window [class=".*"] split toggle # toggles split at each new window
bindsym $mod+shift+q split toggle kill #kill command resets the split
